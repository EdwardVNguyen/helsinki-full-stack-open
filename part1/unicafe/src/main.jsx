// Allows react componentss to manipulate the DOM
import ReactDOM from 'react-dom/client'

import App from './App'

// App will serve as our entry point to our web application
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
